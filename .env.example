# Copy this to .env.local for local development
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: path to Chrome for local puppeteer (if not using puppeteer directly)
# CHROME_PATH=/usr/bin/google-chrome
